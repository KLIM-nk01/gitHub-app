import React from 'react';
import Loader_style from './Loader.module.scss'

const Loader = () => {
    return (
        <div className={Loader_style.loader}>

        </div>
    );
};

export default Loader;